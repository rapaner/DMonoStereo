<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DMonoStereo.Views.SettingsPage"
             Title="Настройки"
             BackgroundColor="{DynamicResource PageBackgroundColor}">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <Border BackgroundColor="{DynamicResource CardBackgroundColor}"
                    Padding="20"
                    StrokeThickness="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="15" />
                </Border.StrokeShape>
                <VerticalStackLayout Spacing="10">
                    <Label Text="ℹ️ О приложении"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="{DynamicResource PrimaryTextColor}" />

                    <Label x:Name="AppNameLabel"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="{DynamicResource PrimaryTextColor}" />

                    <Label x:Name="VersionLabel"
                           FontSize="14"
                           TextColor="{DynamicResource SecondaryTextColor}" />

                    <Label Text="Приложение для управления музыкальной библиотекой. Добавляйте исполнителей, альбомы и треки, ведите статистику."
                           FontSize="14"
                           TextColor="{DynamicResource SecondaryTextColor}" />

                    <Label Text="Команда"
                           FontSize="16"
                           FontAttributes="Bold"
                           Margin="0,10,0,0"
                           TextColor="{DynamicResource PrimaryTextColor}" />

                    <Label Text="Разработка — Дмитрий Брагин"
                           FontSize="14"
                           TextColor="{DynamicResource SecondaryTextColor}" />

                    <Label Text="Дизайнер - Тимофей Мухин"
                           FontSize="14"
                           TextColor="{DynamicResource SecondaryTextColor}" />

                    <Label Text="Идеи и вдохновение — Мария Брагина ❤️"
                           FontSize="14"
                           TextColor="{DynamicResource SecondaryTextColor}" />
                </VerticalStackLayout>
            </Border>

            <Border BackgroundColor="{DynamicResource CardBackgroundColor}"
                    Padding="20"
                    StrokeThickness="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="15" />
                </Border.StrokeShape>
                <VerticalStackLayout Spacing="12">
                    <Label Text="🎨 Внешний вид"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="{DynamicResource PrimaryTextColor}" />

                    <Label Text="Выберите тему приложения"
                           FontSize="14"
                           TextColor="{DynamicResource SecondaryTextColor}" />

                    <Picker x:Name="ThemePicker"
                            Title="Тема"
                            SelectedIndexChanged="OnThemeChanged"
                            TextColor="{DynamicResource PrimaryTextColor}"
                            TitleColor="{DynamicResource SecondaryTextColor}">
                        <Picker.Items>
                            <x:String>🌓 Автоматически (системная)</x:String>
                            <x:String>☀️ Светлая тема</x:String>
                            <x:String>🌙 Темная тема</x:String>
                        </Picker.Items>
                    </Picker>

                    <Label x:Name="ThemeDescriptionLabel"
                           FontSize="12"
                           TextColor="{DynamicResource SecondaryTextColor}" />
                </VerticalStackLayout>
            </Border>

            <Border BackgroundColor="{DynamicResource CardBackgroundColor}"
                    Padding="20"
                    StrokeThickness="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="15" />
                </Border.StrokeShape>
                <VerticalStackLayout Spacing="12">
                    <Label Text="⌨️ Большие буквы"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="{DynamicResource PrimaryTextColor}" />

                    <Label Text="Выберите режим капитализации букв при вводе текста"
                           FontSize="14"
                           TextColor="{DynamicResource SecondaryTextColor}" />

                    <Picker x:Name="CapitalizationPicker"
                            Title="Режим капитализации"
                            SelectedIndexChanged="OnCapitalizationChanged"
                            TextColor="{DynamicResource PrimaryTextColor}"
                            TitleColor="{DynamicResource SecondaryTextColor}">
                        <Picker.Items>
                            <x:String>Каждое слово</x:String>
                            <x:String>Только первая буква</x:String>
                        </Picker.Items>
                    </Picker>

                    <Label x:Name="CapitalizationDescriptionLabel"
                           FontSize="12"
                           TextColor="{DynamicResource SecondaryTextColor}" />
                </VerticalStackLayout>
            </Border>

            <Border BackgroundColor="{DynamicResource CardBackgroundColor}"
                    Padding="20"
                    StrokeThickness="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="15" />
                </Border.StrokeShape>
                <VerticalStackLayout Spacing="12">
                    <Label Text="☁️ Резервное копирование"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="{DynamicResource PrimaryTextColor}" />

                    <Label Text="Создавайте резервные копии вашей коллекции на Яндекс Диске."
                           FontSize="14"
                           TextColor="{DynamicResource SecondaryTextColor}" />

                    <Label x:Name="LastBackupLabel"
                           FontSize="12"
                           TextColor="{DynamicResource SecondaryTextColor}" />

                    <Button Text="Открыть Яндекс Диск"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Clicked="OnOpenYandexDiskClicked" />
                </VerticalStackLayout>
            </Border>

            <Border BackgroundColor="{DynamicResource CardBackgroundColor}"
                    Padding="20"
                    StrokeThickness="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="15" />
                </Border.StrokeShape>
                <VerticalStackLayout Spacing="12">
                    <Label Text="🔧 Действия"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="{DynamicResource PrimaryTextColor}" />

                    <Button Text="Очистить все данные"
                            BackgroundColor="{DynamicResource ErrorColor}"
                            TextColor="{DynamicResource PrimaryTextColor}"
                            CornerRadius="10"
                            Clicked="OnClearDataClicked" />
                </VerticalStackLayout>
            </Border>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>