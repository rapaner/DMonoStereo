# DMonoStereo

Музыкальная библиотека на .NET MAUI для ведения коллекции исполнителей, альбомов и треков с красивым современным UI, локальной базой данных (SQLite, EF Core), поиском по Discogs и резервным копированием на Яндекс Диск.

## 🚀 Возможности

- Управление коллекцией: исполнители, альбомы, треки (добавление, редактирование, удаление)
- Детальные страницы с обложками, рейтингами, годами, статистикой
- Списки со строкой поиска, сортировкой и подгрузкой по мере прокрутки
- Поиск альбомов в Discogs и добавление в коллекцию:
  - Поиск по названию
  - Просмотр версий релиза
  - Импорт обложек и метаданных
- Работа с изображениями: выбор и удаление обложек альбомов
- Темы оформления: светлая, тёмная, автоматическая
- Резервное копирование/восстановление на Яндекс Диск (через OAuth)
- Локальное хранение данных (SQLite + EF Core)

## 📱 Основные экраны

- Все исполнители
- Все альбомы
- Все треки
- Поиск альбомов
- Версии релиза
- Добавление альбома из поиска/из версии
- Карточки: исполнитель, альбом
- Формы: добавить/редактировать исполнителя, альбом, трек
- Настройки
- Яндекс Диск

## 🛠️ Технологии

- .NET MAUI
- SQLite + Entity Framework Core
- Встроенный DI-контейнер
- Discogs API (HTTP-клиент с заголовком авторизации токеном)
- Яндекс Диск (OAuth + API для резервных копий)

## ⚙️ Установка и запуск

1) Установите .NET SDK (версию см. глобальные настройки проекта)
2) Откройте решение в Visual Studio
3) Установите нужную целевую платформу (Android/Windows и т.д.)
4) Запустите проект `DMonoStereo`

Дополнительно:
- Для Discogs нужен токен: задаётся через конфигурацию (`appsettings*.json` как эмбед ресурсы) — ключ `Discogs:Token`.
- При первом запуске база создаётся в `AppDataDirectory` (файл `dmonostereo.db`).

---

## 📚 Процессы в приложении

Ниже перечислены основные сценарии, доступные пользователю, с краткими шагами.

### 1) Добавить исполнителя
- Откройте список исполнителей: Все исполнители → кнопка «Добавить»
- Заполните имя исполнителя
- Сохраните

### 2) Отредактировать/удалить исполнителя
- Откройте «Все исполнители»
- Выберите исполнителя → «Изменить» или действие удаления
- Внесите изменения → «Сохранить» или подтвердите удаление

### 3) Добавить альбом
- Откройте форму «Альбом»
- Укажите исполнителя (поиск по имени с подсказками или добавьте нового)
- Введите название, год, рейтинг
- При необходимости выберите/удалите обложку
- «Сохранить»

### 4) Отредактировать/удалить альбом
- Откройте «Все альбомы»
- Выберите альбом → «Изменить» или действие удаления
- Обновите поля (год, рейтинг, обложка и т.п.) → «Сохранить» или удалите

### 5) Добавить трек
- Откройте нужный альбом или «Все треки»
- «Добавить трек»
- Укажите название, длительность/номер (если предусмотрено), рейтинг (если предусмотрено)
- «Сохранить»

### 6) Отредактировать/удалить трек
- Откройте «Все треки» или карточку альбома
- Выберите трек → «Изменить» или удалите
- «Сохранить»

### 7) Поиск альбомов в Discogs
- Откройте «Поиск альбомов»
- Введите от 3 символов, выполните поиск
- Выберите релиз из списка результатов

### 8) Просмотр и выбор версии релиза (Discogs)
- На странице версий релиза просмотрите варианты
- Выберите нужную версию (год/издание)

### 9) Добавить альбом из поиска/версии Discogs
- На странице добавления из поиска или из версии
- Проверьте и при необходимости скорректируйте поля (исполнитель, название, год, обложка)
- «Сохранить» — альбом добавится в коллекцию

### 10) Работа с обложками альбомов
- В карточке редактирования альбома нажмите «Выбрать обложку»
- Выберите изображение из файлов/галереи
- Чтобы удалить — «Удалить»

### 11) Списки, поиск и сортировка
- На страницах «Все исполнители/альбомы/треки» есть:
  - Поисковая строка для фильтра по имени/названию
  - Сортировка (например, по имени, количеству альбомов, рейтингу)
  - Подгрузка при прокрутке (порог внизу списка)

### 12) Настройки и темы
- Откройте «Настройки»
- Раздел «Внешний вид»: выберите тему — Авто, Светлая, Тёмная
- Раздел «О приложении»: версия, описание и команда
- Раздел «Действия»: очистить все данные (внимательно, действие необратимо)

### 13) Резервное копирование на Яндекс Диск
- Откройте «Настройки» → «Открыть Яндекс Диск» или сразу страницу «Яндекс Диск»
- На странице «Яндекс Диск»:
  - «Открыть браузер для авторизации» и получите OAuth-токен
  - Вставьте токен в поле и «Сохранить токен»
  - «Создать резервную копию» — выгрузит файл базы на Диск
  - «Обновить» для списка бэкапов
  - «Удалить резервную копию» — удалить выбранную
  - Отобразится информация о диске и дате последнего бэкапа

### 14) Восстановление из резервной копии (Яндекс Диск)
- На странице «Яндекс Диск» нажмите «Обновить» и выберите бэкап
- «Восстановить из резервной копии»
- Приложение автоматически создаст локальный резерв перед заменой и восстановит данные

---

## 🔐 Конфигурация Discogs

HTTP‑клиент настраивается с заголовком авторизации на основе токена:
- Ключи конфигурации: `Discogs:Key`, `Discogs:Secret`, `Discogs:Token` (используется токен)
- Базовый адрес API: `https://api.discogs.com/`
- Идентификатор приложения в User-Agent: `DMonoStereo/1.0`

Хранение конфигурации — через встроенные `appsettings*.json`, подключённые как ресурсы сборки.

## 💾 База данных

- Файл: `dmonostereo.db` в `AppDataDirectory`
- Миграции и модель — через `EF Core`

## 🧩 Архитектура (высокоуровнево)

- `Services`: работа с БД (`MusicService`), поиском (`MusicSearchService`), Discogs (`DiscogsService`), изображениями (`ImageService`), резервным копированием (`YandexDiskService`, `YandexOAuthService`), миграции (`DatabaseMigrationService`)
- `Models`: доменные модели и DTO для Discogs/версий/поиска
- `Views`: страницы MAUI (списки, карточки, формы, настройки, Яндекс Диск)